# creates a file from a random list of files and then cats it

import os
from sys import argv 

# cat = os.system('cat')

def badcat(argv):
    target = argv[0]
    # check if file exists
    


# if file already exists, cat a random other file